<html>
<body>

<div>
    <form action="/logout" method="post">
        <input type="submit" value="Sign Out"/>
        <input type="hidden" name="_csrf" value="{{_csrf.token}}">
    </form>
</div>

    <div>
        <form method="post">
            <input type="text" name="nameDevice" placeholder="Ввведите название партии: ">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <button type="submit">Добавить</button>
        </form>
    </div>

<div> Список партий </div>
    <form method="get" action="/main">
        <input type="text" name="filter" placeholder="Введите партию для поиска" >
        <button type="submit">Найти</button>
    </form>

{{#device}}
    <div>
        <b>{{id}}</b>
        <span>{{nameDevice}}</span>
        <strong>{{nameAuthor}}</strong>
        <ul>
            {{#projects}}
            <li>{{title}}
                {{status}}
            </li>
            {{/projects}}
        </ul>
<!--        <datalist>{{projects}}</datalist>-->
    </div>
{{/device}}
</body>
</html>